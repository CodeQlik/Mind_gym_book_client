import ContactClient from "./ContactClient";

export const metadata = {
    title: "Contact Us | Mind Gym Book",
    description: "Get in touch with our team for any queries regarding your orders, book selection, or support.",
};

export default function Contact() {
    return <ContactClient />;
}
