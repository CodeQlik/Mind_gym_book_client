import NewTicketClient from "./NewTicketClient";

export const metadata = {
  title: "Create New Ticket - Mind Gym Book",
  description: "Raise a new support ticket.",
};

export default function NewTicketPage() {
  return <NewTicketClient />;
}
